## copied_files <- sapply(files,
##                        function(file){
##     from <- file.path(extdata_path, file)
##     to <- file.path(getwd(), file)
##     file.copy(from, to, overwrite=TRUE)}
## )
